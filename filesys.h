#include "errors.h"

int can_access(char*, int);

gt_error read_file(int, char*, char*); 

gt_error print_files_in_folder(char*); 

gt_error get_game_folder(char **, char*, char*);

gt_error read_game(int, char*, char*);

gt_error get_create_folder(char**); 

